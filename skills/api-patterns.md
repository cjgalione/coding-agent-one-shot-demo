# API Patterns

- This fixture is frontend-only. Use local component state for demo data.
- Do not add a backend unless the task explicitly requires persistence or server behavior.
- Keep data shapes plain and easy to inspect.
