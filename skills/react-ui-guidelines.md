# React UI Guidelines

- Keep the first screen immediately useful.
- Prefer simple stateful React components over new libraries.
- Make controls real: filters should filter, forms should add records, buttons should do work.
- Use semantic labels and visible text that matches the user's domain.
- Avoid placeholder text, lorem ipsum, dead buttons, and empty dashboard cards.
