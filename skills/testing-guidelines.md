# Testing Guidelines

- Extend the existing Vitest and Testing Library style.
- Test the primary user flow described by the prompt.
- Prefer accessible queries such as labels, roles, and visible text.
- Keep tests deterministic and fast.
