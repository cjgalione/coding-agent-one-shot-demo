export default function App() {
  return (
    <main className="app-shell">
      <section className="placeholder">
        <p className="eyebrow">One-shot app fixture</p>
        <h1>Placeholder app</h1>
        <p>
          This starter screen is intentionally small. The coding agent should
          replace it with a working vertical slice for the requested workflow.
        </p>
      </section>
    </main>
  );
}
